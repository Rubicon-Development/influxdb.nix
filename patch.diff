diff --git a/build.go b/build.go
index 606e949..3331010 100644
--- a/build.go
+++ b/build.go
@@ -1,5 +1,7 @@
 package influxdb
 
+import "strings"
+
 // BuildInfo represents the information about InfluxDB build.
 type BuildInfo struct {
 	Version string // Version is the current git tag with v prefix stripped
@@ -7,11 +9,21 @@ type BuildInfo struct {
 	Date    string // Date is the build date in RFC3339
 }
 
+const buildVersionSuffix = " + Apex-Patches"
+
 var buildInfo BuildInfo
 
+func decorateVersion(version string) string {
+	if version == "" || strings.HasSuffix(version, buildVersionSuffix) {
+		return version
+	}
+
+	return version + buildVersionSuffix
+}
+
 // SetBuildInfo sets the build information for the binary.
 func SetBuildInfo(version, commit, date string) {
-	buildInfo.Version = version
+	buildInfo.Version = decorateVersion(version)
 	buildInfo.Commit = commit
 	buildInfo.Date = date
 }
diff --git a/cmd/influxd/main.go b/cmd/influxd/main.go
index 200b76e..2eec4d9 100644
--- a/cmd/influxd/main.go
+++ b/cmd/influxd/main.go
@@ -73,7 +73,8 @@ func versionCmd() *cobra.Command {
 		Use:   "version",
 		Short: "Print the influxd server version",
 		Run: func(cmd *cobra.Command, args []string) {
-			fmt.Printf("InfluxDB %s (git: %s) build_date: %s\n", version, commit, date)
+			info := influxdb.GetBuildInfo()
+			fmt.Printf("InfluxDB %s (git: %s) build_date: %s\n", info.Version, info.Commit, info.Date)
 		},
 	}
 }
diff --git a/replications/remotewrite/writer.go b/replications/remotewrite/writer.go
index ae1832a..75a7db6 100644
--- a/replications/remotewrite/writer.go
+++ b/replications/remotewrite/writer.go
@@ -188,7 +188,7 @@ func normalizeResponse(r *http.Response, err error) (*http.Response, string, boo
 		return &http.Response{}, errMsg, false
 	}
 
-	return r, errMsg, true
+	return r, errMsg, false
 }
 
 func errorIsTimeout(err error) bool {
